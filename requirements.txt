# Install PyTorch with CUDA FIRST (matched to your GPU/driver), e.g. CUDA 12.4:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
# then:  pip install -r requirements.txt
diffusers
transformers
accelerate
peft
safetensors
pillow
numpy
fastapi
uvicorn[standard]
websockets
